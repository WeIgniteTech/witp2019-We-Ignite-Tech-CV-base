import React from 'react';

//search CV by name

function cvSearch() {
  return (
    <div>
      <form >
        <div>
         <input />
        </div>
        <div>
          <button onClick={() => cvSearch}>Search</button></div>
      </form>
    </div>
    //{cvFound}
  );
}

export default cvSearch;
